spring.application.name=OOP3-assignment-653284

# === API KEYS (replace the placeholders) ===
omdb.api.key=b3dc020d
tmdb.api.key=da61ae8d8b41bd9ad5aa024e927b0c0f  

# === Local image folder (already .gitignored) ===
images.path=images

# === H2 & JPA (unchanged) ===
spring.datasource.url=jdbc:h2:mem:watchlist;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# === Web server port ===
server.port=8081



